@{
    Layout = null;
}
<div class="alert alert-danger">Bạn thực sự muốn đăng xuất khỏi hệ thống?</div>
<div class="form-group">
    <div class="btn-group">
        <a href="@Url.Action("Logout", "Login", new { mode="force" })" class="btn btn-primary"><i class="fa fa-key"></i> Đăng xuất</a>
    </div>
</div>