@if (ViewBag.Error != null)
{
    <div class="alert alert-warning">@Html.Raw(ViewBag.Error)</div>
    return;
}
@{
    if (Request.getValue("layout") == "null") { Layout = null; }
    var idformbuoc1 = "frmbuoc1" + DateTime.Now.toTimestamp().ToString();
    var idtarget = Request.getValue("idtarget");
}
<br />
<h4 class="font-weight-bold">BƯỚC 1: ĐẨY DỮ LIỆU</h4>
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <div class="upload-area" id="uploadfile" data-urlpost="@Url.Action("Buoc2", "bcTuan", new { area = "", layout = "null", idtarget=idtarget })" data-target="@idtarget">
                    <p class="text-center">Kéo và thả tập tin Excel 2007 trở lên (Type of file: Microsoft Excel Worksheet) vào khu vực này hoặc nhấp để chọn file</p>
                </div>
                <div id="fileDetails" class="mt-4">
                    <h5>Danh sách tập tin đã chọn:</h5>
                    <ul id="fileList" class="list-group"></ul>
                </div>
                <button id="uploadButton" class="btn btn-primary mt-3" disabled><i class="fa fa-plus"></i> Đẩy lên máy chủ</button>
            </div>
        </div>
    </div>
</div>
<div class="alert alert-info">
    Dữ liệu đầu vào của BC tuần
    <br />1- B02-00. Thống kê KCB (tháng). Liệt kê các tỉnh. Từ tháng 1 đến tháng năm BC, các tùy chọn khác để mặc định
    <br />2- B02-10. Thống kê KCB (tháng). Liệt kê các CSYT trong tỉnh (bấm chữ tên tỉnh có link xanh để lấy). Từ tháng 1 đến tháng năm BC, các tùy chọn khác để mặc định
    <br />3- B04-00. Thống kê chi bình quân tháng.  Liệt kê các tỉnh. Từ tháng 1 đến tháng năm BC. Các tùy chọn khác để mặc định
    <br />4- B26-00. Thống kê gia tăng chi phí KCB BHYT theo NĐ75 (theo ngày nhận).  Liệt kê các tỉnh. Thời gian đến ngày BC. Các tùy chọn khác mặc định.
    <br />5- B26-10. Thống kê gia tăng chi phí KCB BHYT theo NĐ75 (theo ngày nhận). Liệt kê các CSYT trong tỉnh (bấm chữ tên tỉnh có link xanh để lấy). Thời gian đến ngày BC. Các tùy chọn khác mặc định.
</div>
<script>
    $(document).ready(function () { drapDropFiles(".xlsx,.zip"); });
</script>